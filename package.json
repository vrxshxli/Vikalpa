{
  "name": "vikalpa",
  "version": "1.0.0",
  "private": true,
  "description": "VIKALPA — travel resilience and disruption recovery engine",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix mobile install",
    "server": "npm --prefix server run start",
    "server:dev": "npm --prefix server run dev",
    "mobile": "npm --prefix mobile run start",
    "snapshot": "npm --prefix server run snapshot",
    "typecheck": "npm --prefix server run typecheck && npm --prefix mobile run typecheck"
  }
}
